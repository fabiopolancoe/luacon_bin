-- Configuration file

VERSION = '1.0'

--[[

  ENVIRONMENT = The environment to use
    -- script > Inherits the environment of the script you load and commands you enter
    -- none > Resets the environment after every command
    -- file.lua > Uses the environment of the script specified
  
  EXIT_IF_EMPTY = Exit luacon if the input entered is an empty string
    -- true
    -- false

  EXIT_AFTER_EXECUTION = Exit luacon after running the code specified
    -- true
    -- false

]]

ENVIRONMENT = 'script' -- default
EXIT_IF_EMPTY = true -- default
EXIT_AFTER_EXECUTION = false -- default

--[[
  Warnings and other settings
]]

CHANGED_NAME_WARNING = true